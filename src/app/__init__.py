"""
Прикладной слой: то, с чем разговаривает представление.

Здесь живут контроллеры сценариев — «загрузить модель», «собрать мод»,
«извлечь текстуру». Они владеют фоновыми воркерами (`src/services`), меняют
состояние сеанса (`src/domain`) и эмитят события. Того, кто рисует, они не
знают: подписчиком может быть Qt-панель, а может быть мост в веб-фронт.

Правило слоёв:

    ui  →  app  →  services / domain

Обратных импортов нет: `src/app` не импортирует `src.ui` и не трогает Qt.
"""
